-- 코드를 입력하세요
SELECT a.NAME, b.COUNT AS COUNT
FROM ANIMAL_INS a
JOIN (
    SELECT name, COUNT(NAME) AS COUNT
    FROM ANIMAL_INS
    GROUP BY name
) b ON a.NAME = b.NAME 
WHERE b.COUNT >= 2
GROUP BY name
ORDER BY a.name;